{% extends 'base.html.twig' %}

{% block body %}
<section id="page-top">
    <!-- Header -->
    <header class="masthead">
        <div class="container">
            <div class="intro-text">
                <div class="intro-lead-in">Laisser un commentaire</div>
                <div class="intro-lead-in"><a class="navbar-brand logo" href="{{ path('homepage') }}"> OzOut</a></div>
            </div>
        </div>
    </header>

    {#<!-- Portfolio Grid -->
    <section class="bg-light" id="portfolio">
        <div class="container formCreateEvent">
            <h1 align="center">Créer un événement</h1>
            <div class="offset-md-3 col-md-6">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <img width="200" id="previewPictureEvent" src="#" alt="">
                <div>
                    <input type="submit" value="Create" class="btn btn-primary btn-xl text-uppercase js-scroll-trigger btn-header btn-create"/>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
        </div>
    </section>
</section>#}

  {#  <header class="masthead">
<div class="container formCreateEvent">
    <h1 align="center">Laisser un commentaire</h1>
    <h2 align="center"></h2>#}
    <section class="bg-light" id="portfolio">
        <div class="container formCreateEvent">
            <h1 align="center">Laisser un commentaire</h1>
            <div class="offset-md-3 col-md-6">
    {{ form_start(form) }}

    <div>
        <div>
            {{ form_label (form.event) }}
            {{ form_widget (form.event) }}
        </div>
       <div> {{ form_label (form.score) }}
             {{ form_widget (form.score) }}
        {#Rating stars#}
            <div class="rating text-center">
                <span data-score="5">★</span><span data-score="4">★</span><span class="active" data-score="3">★</span><span data-score="2">★</span><span data-score="1">★</span>
            </div>
       </div>
        {#end Rating stars#}
        <div>
            {{ form_label (form.comment) }}
            {{ form_widget (form.comment) }}
        </div>
        <div>
            {{ form_label (form.user) }}
            {{ form_widget (form.user) }}
        </div>
    </div>
    <div>
        <input type="submit" value="Valide ton avis" class="btn btn-primary btn-xl text-uppercase js-scroll-trigger btn-header" />
    </div>
            {{ form_end(form) }}

    {#<div align="center">
        <a href="{{ path('review_index') }}" class="btn btn-primary btn-xl text-uppercase js-scroll-trigger btn-header">Retour à la liste des avis</a>
    </div>#}
</section
</section>
{% endblock %}
{% block javascripts %}
    <!-- Custom scripts for this template -->
    <script src="{{ asset('assets/js/review.js') }}"></script>
{% endblock %}
